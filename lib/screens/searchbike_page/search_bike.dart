import 'package:flutter/material.dart';

class SearchBikeList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Search Bike"),
    );
  }
}